# 🚀 Инструкция: Как запустить сайт на GitHub Pages

## Проблема
После пуша на GitHub вы видите только содержимое README.md вместо самого сайта.

## Решение (3 простых шага):

### Шаг 1: Включите GitHub Pages в настройках репозитория

1. Откройте ваш репозиторий на GitHub: https://github.com/Abobusmogus/osint4us
2. Перейдите в **Settings** (вкладка вверху)
3. В левом меню найдите и нажмите **Pages**
4. В разделе **"Source"** выберите **"GitHub Actions"** (НЕ "Deploy from a branch")
5. Нажмите **Save**

### Шаг 2: Запустите деплой

Выполните в PowerShell:

```powershell
cd c:\Users\qwilu\osint4us-site

# Добавьте изменения
git add package.json

# Создайте коммит
git commit -m "Fix homepage URL for GitHub Pages"

# Запушьте на GitHub
git push origin main
```

### Шаг 3: Проверьте деплой

1. Перейдите во вкладку **Actions** в вашем репозитории на GitHub
2. Вы увидите запущенный workflow "Deploy to GitHub Pages"
3. Подождите 2-3 минуты, пока он завершится (зелёная галочка = успех)
4. После завершения ваш сайт будет доступен по адресу:
   **https://Abobusmogus.github.io/osint4us**

## ⚠️ Важно!

- Первый деплой может занять 3-5 минут
- Если workflow завершился с ошибкой - проверьте логи в Actions
- После деплоя подождите 1-2 минуты перед проверкой сайта
- Если сайт не открывается - очистите кэш браузера (Ctrl+Shift+Delete)

## Альтернатива: Ручной деплой через gh-pages

Если GitHub Actions не работает, используйте:

```powershell
cd c:\Users\qwilu\osint4us-site
npm run deploy
```

Затем в Settings → Pages выберите ветку `gh-pages` как источник.
