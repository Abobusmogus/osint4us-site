# 🚀 Быстрый деплой на GitHub Pages

## Проблема: GitHub показывает сырой репозиторий вместо сайта

Это происходит потому, что GitHub Pages нужно указать, откуда брать файлы.

## Решение 1: Использовать gh-pages (САМЫЙ ПРОСТОЙ)

### Шаг 1: Установите gh-pages
```bash
cd C:\Users\qwilu\osint4us-site
npm install --save-dev gh-pages
```

### Шаг 2: Задеплойте
```bash
npm run deploy
```

Это автоматически:
- Соберёт проект
- Создаст ветку `gh-pages`
- Запушет собранные файлы
- Настроит GitHub Pages

### Шаг 3: Включите GitHub Pages
1. Зайдите в Settings → Pages
2. В разделе "Source" выберите ветку `gh-pages`
3. Папка должна быть `/ (root)`
4. Сохраните

**Готово!** Сайт будет доступен через несколько минут.

---

## Решение 2: Использовать GitHub Actions (уже настроено)

### Проверьте настройки:
1. Зайдите в Settings → Pages
2. В разделе "Source" выберите **"GitHub Actions"** (не "Deploy from a branch")
3. Сохраните

### Проверьте, запустился ли workflow:
1. Зайдите во вкладку **Actions** в репозитории
2. Должен быть запущен workflow "Deploy to GitHub Pages"
3. Если он не запустился - сделайте новый коммит и пуш:
```bash
git commit --allow-empty -m "Trigger GitHub Actions"
git push
```

---

## Решение 3: Ручной деплой через ветку gh-pages

```bash
cd C:\Users\qwilu\osint4us-site

# Соберите проект
npm run build

# Создайте ветку gh-pages
git checkout --orphan gh-pages
git rm -rf .

# Скопируйте содержимое build в корень
# (вручную скопируйте все файлы из build/ в корень репозитория)

git add .
git commit -m "Deploy to GitHub Pages"
git push origin gh-pages
```

---

## ⚠️ Важно!

После деплоя подождите 1-2 минуты - GitHub нужно время на обновление.

Если сайт всё ещё показывает репозиторий:
1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Попробуйте открыть в режиме инкогнито
3. Проверьте URL - должен быть `https://username.github.io/repo-name/`
